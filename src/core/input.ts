

export class Input {


 constructor() {
     /*
     const canvas = $WGPU.canvas;
     canvas.onmousemove = (e) => {
         console.log(e.movementX, e.movementY)
        $WGPU.mainCamera.rotate(e.movementX );
     }

      */
 }
}
